doctype html
html(lang="en")
	head
		title="Broadcast | Media Streaming"
		script(src="peerjs@1.3.1.min.js")
		script(src="socket-io/socket.io.js")
		script(src="broadcaster.js")
	body
		script.
			let peerServer = { host: window.location.hostname, port: 3000, path: '/peerjs' }
			let broadcaster = new Broadcaster({ peerServer })
			broadcaster.start()

			// setTimeout(() => broadcaster.pause(), 10000)
			// setTimeout(() => broadcaster.resume(), 20000)
			// setTimeout(() => broadcaster.stop(), 30000)
